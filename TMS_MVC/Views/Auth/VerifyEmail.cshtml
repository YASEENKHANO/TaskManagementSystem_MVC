
@{
    ViewBag.Title = "VerifyEmail";
}

@{
    ViewBag.Title = "Email Verification";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow p-4 text-center">
                <h2 class="text-success"><i class="bi bi-envelope-check"></i> Email Verified</h2>
                <p class="lead">@ViewBag.Message</p>
                <a href="@Url.Action("Login", "Login")" class="btn btn-primary"><i class="bi bi-box-arrow-in-right"></i> Go to Login</a>
            </div>
        </div>
    </div>
</div>
